name: watermark-removal
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      # PyTorch CUDA 12.4 wheels (Ada / sm_89). Do NOT mix with conda pytorch channel.
      # Pins from https://pytorch.org/get-started/previous-versions/ (v2.6.0 + cu124).
      - --extra-index-url https://download.pytorch.org/whl/cu124
      - torch==2.6.0
      - torchvision==0.21.0
      - torchaudio==2.6.0
      - diffusers==0.32.2
      - transformers==4.49.0
      - accelerate==1.4.0
      - lpips==0.1.4
      - numpy==2.1.3
      - pandas==2.2.3
      - matplotlib==3.10.0
      - pyyaml==6.0.2
      - tqdm==4.67.1
      - scikit-image==0.25.1
      - opencv-python==4.11.0.86
      - pillow==11.1.0
      - requests==2.32.3
      - huggingface_hub==0.28.1
      - safetensors==0.5.2
      - einops==0.8.1
